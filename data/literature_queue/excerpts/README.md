# Sample Excerpts

These are short sample excerpts for testing the local packet builder.

They are not full papers, not theorem verification, and not external literature rows. They feed the future LLM/manual extraction pipeline by providing local text snippets for deterministic packet generation.
