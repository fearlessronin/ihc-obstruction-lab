# Notebooks

This directory is reserved for exploratory notebooks.

Notebooks should use the package APIs in `src/ihc_lab` and should not become the authoritative source of dataset rows. Promote reusable code into the package and keep committed notebook outputs small.
